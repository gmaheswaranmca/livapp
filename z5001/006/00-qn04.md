# Path Sum II

**Categories:** Tree,Depth-First Search,Backtracking,Binary Tree | **Tags:** tree,binary-tree,dfs,backtracking,recursion | **Companies:** Amazon,Microsoft,Google,Adobe,Apple,Facebook,Bloomberg

## Description
Given the root of a binary tree and an integer targetSum, return all root-to-leaf paths where the sum of the node values equals targetSum. Each path should be returned as a list of node values.

**Time Complexity:** O(n)
**Space Complexity:** O(h)

### Input Format
The first line contains the level-order traversal of the binary tree where -1 represents a null node. The second line contains the integer targetSum.

### Output Format
Return all root-to-leaf paths whose sum equals targetSum. Each path is represented as a list of integers.

### Explanation
Perform a depth-first traversal while maintaining the current path and remaining sum. Whenever a leaf node is reached and the path sum equals targetSum, add the current path to the answer.

### Sample Case 1
- **Input:** `root = [5,4,8,11,null,13,4,7,2,null,null,5,1], targetSum = 22`
- **Output:** `[[5,4,11,2],[5,8,4,5]]`

### Sample Case 2
- **Input:** `root = [1,2,3], targetSum = 5`
- **Output:** `[]`

### Constraints
- The number of nodes in the tree is in the range [0, 5000].
- -1000 <= Node.val <= 1000
- -1000 <= targetSum <= 1000

### Hints
- Use Depth-First Search (DFS).
- Maintain the current path while traversing.
- Backtrack after visiting each child.
- Only root-to-leaf paths are considered valid.

### Test Cases
| Input | Output | Hidden |
|---|---|---|
| `[5,4,8,11,-1,13,4,7,2,-1,-1,5,1] 22` | `[[5,4,11,2],[5,8,4,5]]` | False |
| `[1,2,3] 5` | `[]` | False |
| `[1,2] 1` | `[]` | True |
| `[1] 1` | `[[1]]` | True |
| `[] 0` | `[]` | True |
| `[1,-2,-3,1,3,-2,-1] -1` | `[[1,-2,1,-1]]` | True |
| `[2] 3` | `[]` | True |
| `[5,4,8,11,-1,13,4,7,2,-1,-1,-1,1] 22` | `[[5,4,11,2]]` | True |
| `[10,5,12,4,7] 22` | `[[10,5,7],[10,12]]` | True |
| `[1,2,3,4,5,6,7] 100` | `[]` | True |

### Boilerplate Code
**Cpp**
```cpp
#include <bits/stdc++.h>
using namespace std;

struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

class Solution {
public:
    vector<vector<int>> pathSum(TreeNode* root, int targetSum) {
        // Students write code here
    }
};

TreeNode* buildTree(const vector<int>& nodes) {
    if (nodes.empty() || nodes[0] == -1) return nullptr;

    TreeNode* root = new TreeNode(nodes[0]);
    queue<TreeNode*> q;
    q.push(root);

    int i = 1;
    while (!q.empty() && i < nodes.size()) {
        TreeNode* curr = q.front();
        q.pop();

        if (i < nodes.size() && nodes[i] != -1) {
            curr->left = new TreeNode(nodes[i]);
            q.push(curr->left);
        }
        i++;

        if (i < nodes.size() && nodes[i] != -1) {
            curr->right = new TreeNode(nodes[i]);
            q.push(curr->right);
        }
        i++;
    }

    return root;
}

int main() {
    int n;
    cin >> n;

    vector<int> nodes(n);
    for (int i = 0; i < n; i++)
        cin >> nodes[i];

    int targetSum;
    cin >> targetSum;

    TreeNode* root = buildTree(nodes);

    Solution obj;
    vector<vector<int>> ans = obj.pathSum(root, targetSum);

    cout << ans.size() << "\n";
    for (auto &path : ans) {
        for (int i = 0; i < path.size(); i++) {
            if (i) cout << " ";
            cout << path[i];
        }
        cout << "\n";
    }

    return 0;
}
```

**Java**
```java
import java.util.*;

class TreeNode {
    int val;
    TreeNode left, right;
    TreeNode(int x) { val = x; }
}

class Solution {
    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {
        // Students write code here
        return new ArrayList<>();
    }
}

public class Main {

    static TreeNode buildTree(int[] nodes) {
        if (nodes.length == 0 || nodes[0] == -1) return null;

        TreeNode root = new TreeNode(nodes[0]);
        Queue<TreeNode> q = new LinkedList<>();
        q.offer(root);

        int i = 1;
        while (!q.isEmpty() && i < nodes.length) {
            TreeNode curr = q.poll();

            if (i < nodes.length && nodes[i] != -1) {
                curr.left = new TreeNode(nodes[i]);
                q.offer(curr.left);
            }
            i++;

            if (i < nodes.length && nodes[i] != -1) {
                curr.right = new TreeNode(nodes[i]);
                q.offer(curr.right);
            }
            i++;
        }

        return root;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int[] nodes = new int[n];
        for (int i = 0; i < n; i++)
            nodes[i] = sc.nextInt();

        int targetSum = sc.nextInt();

        TreeNode root = buildTree(nodes);

        Solution obj = new Solution();
        List<List<Integer>> ans = obj.pathSum(root, targetSum);

        System.out.println(ans.size());
        for (List<Integer> path : ans) {
            for (int i = 0; i < path.size(); i++) {
                if (i > 0) System.out.print(" ");
                System.out.print(path.get(i));
            }
            System.out.println();
        }
    }
}
```

**Python**
```python
from collections import deque

class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left = None
        self.right = None

class Solution:
    def pathSum(self, root, targetSum):
        # Students write code here
        pass

def buildTree(nodes):
    if not nodes or nodes[0] == -1:
        return None

    root = TreeNode(nodes[0])
    q = deque([root])
    i = 1

    while q and i < len(nodes):
        curr = q.popleft()

        if i < len(nodes) and nodes[i] != -1:
            curr.left = TreeNode(nodes[i])
            q.append(curr.left)
        i += 1

        if i < len(nodes) and nodes[i] != -1:
            curr.right = TreeNode(nodes[i])
            q.append(curr.right)
        i += 1

    return root

n = int(input())
nodes = list(map(int, input().split()))
targetSum = int(input())

root = buildTree(nodes)
obj = Solution()
ans = obj.pathSum(root, targetSum)

print(len(ans))
for path in ans:
    print(*path)
```

