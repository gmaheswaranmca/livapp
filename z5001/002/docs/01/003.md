# Contains Duplicate

Check whether an array contains duplicate elements.

Example:

```text
Input: [1,2,3,1]
Output: True
```

```text
Input: [1,2,3,4]
Output: False
```

---

# Common Solution Patterns

## 1. Brute Force Pattern

### Idea

Compare every element with every other element.

### Pattern Used

* Nested Loop
* Comparison Pattern

### Time Complexity

* O(n²)

### Space Complexity

* O(1)

### Code

```python
def contains_duplicate(nums):
    n = len(nums)

    for i in range(n):
        for j in range(i + 1, n):
            if nums[i] == nums[j]:
                return True

    return False


print(contains_duplicate([1,2,3,1]))
```

---

# 2. Sorting Pattern

### Idea

If duplicates exist, after sorting they become adjacent.

### Pattern Used

* Sorting
* Adjacent Comparison

### Time Complexity

* O(n log n)

### Space Complexity

* O(1) or O(n) depending on sorting implementation

### Code

```python
def contains_duplicate(nums):
    nums.sort()

    for i in range(1, len(nums)):
        if nums[i] == nums[i - 1]:
            return True

    return False


print(contains_duplicate([1,2,3,1]))
```

---

# 3. Hash Set Pattern (Optimized)

### Idea

Store visited elements in a set.

If element already exists in set → duplicate found.

### Pattern Used

* Hashing
* Lookup Optimization

### Time Complexity

* O(n)

### Space Complexity

* O(n)

### Code

```python
def contains_duplicate(nums):
    seen = set()

    for num in nums:
        if num in seen:
            return True

        seen.add(num)

    return False


print(contains_duplicate([1,2,3,1]))
```

---

# 4. Using Python Set Directly

### Idea

Set stores only unique values.

If size changes → duplicates exist.

### Pattern Used

* Built-in Hashing

### Time Complexity

* O(n)

### Space Complexity

* O(n)

### Code

```python
def contains_duplicate(nums):
    return len(nums) != len(set(nums))


print(contains_duplicate([1,2,3,1]))
```

---

# Pattern Comparison

| Pattern              | Time       | Space | Interview Usage                | Recommended |
| -------------------- | ---------- | ----- | ------------------------------ | ----------- |
| Brute Force          | O(n²)      | O(1)  | Basic understanding            | ❌           |
| Sorting              | O(n log n) | O(1)  | Useful when sorting allowed    | ⚠️          |
| Hash Set             | O(n)       | O(n)  | Most common interview solution | ✅           |
| Python One-Liner Set | O(n)       | O(n)  | Best Pythonic solution         | ✅✅          |

---

# Optimized Solution Pattern

## Best Pattern → Hash Set

Why?

* Fast lookup: O(1)
* Single traversal
* Most asked in interviews
* Clean logic
* Works efficiently for large inputs

---

# Final Optimized Code

```python
def contains_duplicate(nums):
    seen = set()

    for num in nums:
        if num in seen:
            return True

        seen.add(num)

    return False


nums = [1,2,3,1]

print(contains_duplicate(nums))
```

---

# Interview Explanation

> “I used the Hash Set pattern because set lookup is O(1).
> While traversing the array, if an element already exists in the set, duplicate is found immediately.
> This gives O(n) time complexity with O(n) extra space.”

---

# Related Interview Patterns

* Hashing
* Frequency Counting
* Set Lookup
* Duplicate Detection
* Array Traversal

---

# Similar Problems

* Two Sum
* Valid Anagram
* Happy Number
* Longest Consecutive Sequence
* Group Anagrams
